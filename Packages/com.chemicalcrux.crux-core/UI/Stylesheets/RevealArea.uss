RevealArea.revealed {
    display: flex;
}

RevealArea.unrevealed {
    display: none;
}

RevealArea {
    background-color: rgba(255, 255, 255, 0.05);
    padding: 4px;
    padding-left: 8px;
}

RevealArea > .unity-base-field__input
{
    display: none;
}