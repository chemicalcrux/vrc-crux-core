RevealArea.revealed {
    display: flex;
}

RevealArea.unrevealed {
    display: none;
}

RevealArea {
    padding: 4px;
    padding-left: 8px;
}

RevealArea > .unity-base-field__input
{
    display: none;
}